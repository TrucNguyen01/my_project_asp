@model List<WebApplication4.Models.NhaCungCap>
@{
    Layout = "~/Areas/Admin/Views/Shared/LayoutAdmin.cshtml";
}
<div class="container" id="provide">
    <div>
        <a href="/Admin/NhaCungCap/Insert"><button style="margin: 15px; padding: 4px 10px; border: none" class="pull-right bg-primary"><i class="fa fa-plus"></i> Thêm mới</button></a>
    </div>
    <table class="table table-hover">
        <caption></caption>
        <thead>
            <tr>
                <th>ID</th>
                <th>Tên nhà cung cấp</th>
                <th>Địa chỉ</th>
                <th>Số điện thoại</th>
                <th>Ngày tạo</th>
                <th>Sửa</th>
                <th>Xoá</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr id="row_@item.id_nhacungcap">
                    <td>@item.id_nhacungcap</td>
                    <td>@item.TenNhaCungCap</td>
                    <td>@item.DiaChi</td>
                    <td>@item.SoDienThoai</td>
                    <td>@item.NgayTao.ToString("dd - MM - yyyy")</td>
                    <td><button class="bg-warning"><i class="fa fa-wrench">@Html.ActionLink(" Chỉnh sửa", "Update", new { id = item.id_nhacungcap, controller = "NhaCungCap" })</i></button></td>
                    <td>
                            <button data-id="@item.id_nhacungcap" class="bg-danger btnDelete"><i class="fa fa-trash-o"> Xoá</i></button>
                    </td>
                </tr>
            }
            <tr>
                <th colspan="7">
                    <ul class="pagination">
                        @for (int i = 0; i < ViewBag.SoTrang; i++)
                        {
                            if ((i + 1) == ViewBag.Index)
                            {
                                <li class="active">
                                    <a class="btn bg-primary text-white" href="/Admin/NhaCungCap/Index?index=@(i + 1)">@(i + 1)</a>
                                </li>
                            }
                            else
                            {
                                <li>
                                    <a class="btn bg-body-secondary text-white" href="/Admin/NhaCungCap/Index?index=@(i + 1)">@(i + 1)</a>
                                </li>
                            }
                        }
                    </ul>
                </th>
            </tr>
        </tbody>
    </table>
</div>